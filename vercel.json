{
  "installCommand": "pnpm install --no-frozen-lockfile",
  "buildCommand": "pnpm prisma generate && pnpm build",
  "framework": "nextjs",
  "crons": [
    { "path": "/api/cron/overdue", "schedule": "0 1 * * *" },
    { "path": "/api/cron/recurring", "schedule": "15 1 * * *" }
  ]
}
